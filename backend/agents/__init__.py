"""Food recall agent pipeline package.

Provides LLM-driven processing stages (translation, summarization, structuring),
validation, conversion to alert models, and the LangGraph pipeline that orchestrates
scraped recall records into structured ``FoodRecallAlertCreate`` objects.
"""
